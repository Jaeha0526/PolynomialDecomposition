# Auto-mirrored from pyproject.toml for `pip install -r requirements.txt`
# fallbacks (e.g. Colab). The authoritative source of truth is pyproject.toml;
# regenerate this file with `poetry export -f requirements.txt --output requirements.txt`.
torch>=2.0.0
transformers==4.46.3
trl==0.16.0
datasets
safetensors
sympy>=1.12
numpy>=1.21.0
tqdm
matplotlib
wandb>=0.15.0
